# Warcry CMS security hardening
# Writable folders must never execute scripts.
Options -Indexes
<FilesMatch "\.(php|phtml|phar|php[0-9]|cgi|pl|py|sh|bash|asp|aspx|jsp|exe|dll|com|bat|cmd)$">
    Require all denied
</FilesMatch>
<IfModule mod_php.c>
    php_flag engine off
</IfModule>
